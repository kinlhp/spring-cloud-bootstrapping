server:
  port: 8084
spring:
  application:
    name: consumer
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false
    loadbalancer:
      ribbon:
        # Or remove spring-cloud-starter-netflix-ribbon from your project.
        enabled: false
  profiles:
    active:
      - default
