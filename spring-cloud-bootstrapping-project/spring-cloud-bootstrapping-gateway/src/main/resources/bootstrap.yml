eureka:
  client:
    service-url:
      defaultZone: http://discovery/eureka/
spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: configuration
      name: gateway
