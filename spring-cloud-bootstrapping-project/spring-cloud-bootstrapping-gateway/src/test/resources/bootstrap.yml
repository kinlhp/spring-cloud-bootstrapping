server:
  port: 8080
spring:
  application:
    name: gateway
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false
    loadbalancer:
      ribbon:
        # Or remove spring-cloud-starter-netflix-ribbon from your project.
        enabled: false
  profiles:
    active:
      - default
