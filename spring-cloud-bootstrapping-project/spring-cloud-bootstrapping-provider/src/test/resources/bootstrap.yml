server:
  port: 8083
spring:
  application:
    name: provider
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false
    loadbalancer:
      ribbon:
        # Or remove spring-cloud-starter-netflix-ribbon from your project.
        enabled: false
  profiles:
    active:
      - default
